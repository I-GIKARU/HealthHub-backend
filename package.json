{
  "name": "healthhub-backend",
  "version": "1.0.0",
  "description": "JSON server for HealthHub app",
  "main": "db.json",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
